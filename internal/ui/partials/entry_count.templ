package partials

import "fmt"

templ EntryCount(count int) {
	<span id="entry-count" hx-swap-oob="true" class="text-sm" style="color: var(--color-ink-lighter);">
		{ fmt.Sprintf("%d", count) }
		if count == 1 {
			entry
		} else {
			entries
		}
	</span>
}
